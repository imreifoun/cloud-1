# MySQL Configuration
MYSQL_ROOT_PASSWORD={{ lookup('password', '/tmp/mysql_root chars=ascii_letters,digits length=32') }}
MYSQL_DATABASE=wordpress
MYSQL_USER=wpuser
MYSQL_PASSWORD={{ lookup('password', '/tmp/mysql_user chars=ascii_letters,digits length=32') }}

# Domain
DOMAIN={{ domain_name }}
